-- The active secure Home Health referral workflow uses HomeHealthReferral.
-- HomeHealthReferralSubmission was an intermediate draft table and is no longer referenced.
DROP TABLE IF EXISTS "HomeHealthReferralSubmission" CASCADE;
