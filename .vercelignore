# Vercel hosts only the generated static website in dist-web.
# Keep the backend source out of Vercel's automatic /api function discovery.
api/src/**
api/prisma/**
prisma/**
Dockerfile
railway.json

# Local and repository-only files
.git/**
.github/**
docs/**
*.log
