{
  "name": "spire-ehr",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "db:validate": "prisma validate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "api:install": "npm install --prefix api",
    "api:dev": "npm run dev --prefix api",
    "api:typecheck": "npm run typecheck --prefix api",
    "start": "npm run start --prefix api"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "bcryptjs": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "prisma": "^6.12.0",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3"
  }
}
