{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": [],
    "startCommand": "node --input-type=module -e 'import { PrismaClient } from \"@prisma/client\"; import http from \"node:http\"; const p=new PrismaClient(); try { const target=await p.$queryRawUnsafe(\"SELECT id,migration_name,checksum,started_at,finished_at,rolled_back_at,applied_steps_count,logs FROM \\\"_prisma_migrations\\\" WHERE migration_name=$1 ORDER BY started_at\",\"20260820115500_applicant_lifecycle_core\"); const family=await p.$queryRawUnsafe(\"SELECT migration_name,started_at,finished_at,rolled_back_at,applied_steps_count,logs FROM \\\"_prisma_migrations\\\" WHERE migration_name LIKE $1 ORDER BY started_at\",\"20260820115500%\"); console.log(\"[migration-readonly] target=\"+JSON.stringify(target)); console.log(\"[migration-readonly] family=\"+JSON.stringify(family)); } catch(e){ console.error(\"[migration-readonly] ERROR\",e); process.exitCode=1; } finally { await p.$disconnect(); } http.createServer((req,res)=>{res.writeHead(200,{\"content-type\":\"text/plain\"});res.end(\"inspection complete\");}).listen(Number(process.env.PORT||3000),\"0.0.0.0\",()=>console.log(\"[migration-readonly] probe ready\"));'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "NEVER"
  }
}
