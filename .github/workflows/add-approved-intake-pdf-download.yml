name: Approved Intake PDF Installer (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Approved intake PDF feature is already installed."
