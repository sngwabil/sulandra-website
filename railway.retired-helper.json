{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": [],
    "startCommand": "sh -c 'echo retired-db-helper; exit 0'",
    "restartPolicyType": "NEVER"
  }
}
