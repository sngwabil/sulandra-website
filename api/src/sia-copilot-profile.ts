import { randomUUID } from 'node:crypto';
import type { Express, RequestHandler, Response } from 'express';
import { PrismaClient, UserRole } from '@prisma/client';
import { z } from 'zod';
type AuthContext={userId:string;organizationId:string;role:UserRole;email?:string};type Dependencies={app:Express;prisma:PrismaClient;authOf:(response:Response)=>AuthContext;requireRoles:(...roles:UserRole[])=>RequestHandler};
type IdentitySnapshot={displayName:string|null;workEmail:string|null;employeeUsername:string|null;role:string};type CopilotPreferences={preferredName:string|null;responseStyle:'CONCISE'|'GUIDED'|'DETAILED';proactiveHints:boolean;rememberRecentApps:boolean};type CopilotRecentContext={lastPage:string|null;lastApplication:string|null;lastPageTitle:string|null;recentApplications:string[]};type CopilotProfile={id:string;organizationId:string;userId:string;profileVersion:number;identitySnapshot:IdentitySnapshot;preferences:CopilotPreferences;recentContext:CopilotRecentContext;createdAt:string|Date;updatedAt:string|Date;lastSeenAt:string|Date};
const allRoles=Object.values(UserRole) as UserRole[];const DEFAULT_PREFERENCES:CopilotPreferences={preferredName:null,responseStyle:'GUIDED',proactiveHints:true,rememberRecentApps:true};const DEFAULT_CONTEXT:CopilotRecentContext={lastPage:null,lastApplication:null,lastPageTitle:null,recentApplications:[]};
const updateSchema=z.object({preferredName:z.string().trim().min(1).max(80).nullable().optional(),responseStyle:z.enum(['CONCISE','GUIDED','DETAILED']).optional(),proactiveHints:z.boolean().optional(),rememberRecentApps:z.boolean().optional()});const contextSchema=z.object({page:z.string().trim().min(1).max(240).optional(),application:z.string().trim().min(1).max(160).optional(),pageTitle:z.string().trim().min(1).max(180).optional()});
let profileReadyPromise:Promise<void>|null=null;const ensureProfileStorage=(prisma:PrismaClient)=>profileReadyPromise??=(async()=>{await prisma.$executeRawUnsafe(`CREATE TABLE IF NOT EXISTS "SIAEmployeeProfile" ("id" TEXT PRIMARY KEY,"organizationId" TEXT NOT NULL,"userId" TEXT NOT NULL,"profileVersion" INTEGER NOT NULL DEFAULT 1,"identitySnapshot" JSONB NOT NULL DEFAULT '{}'::jsonb,"preferences" JSONB NOT NULL DEFAULT '{}'::jsonb,"recentContext" JSONB NOT NULL DEFAULT '{}'::jsonb,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"lastSeenAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await prisma.$executeRawUnsafe(`CREATE UNIQUE INDEX IF NOT EXISTS "SIAEmployeeProfile_identity_idx" ON "SIAEmployeeProfile"("organizationId","userId")`);})().catch(error=>{profileReadyPromise=null;throw error});
const normalizePreferences=(value:unknown):CopilotPreferences=>{const input=value&&typeof value==='object'?value as Record<string,unknown>:{};const responseStyle=['CONCISE','GUIDED','DETAILED'].includes(String(input.responseStyle))?String(input.responseStyle) as CopilotPreferences['responseStyle']:DEFAULT_PREFERENCES.responseStyle;return{preferredName:typeof input.preferredName==='string'&&input.preferredName.trim()?input.preferredName.trim().slice(0,80):null,responseStyle,proactiveHints:typeof input.proactiveHints==='boolean'?input.proactiveHints:true,rememberRecentApps:typeof input.rememberRecentApps==='boolean'?input.rememberRecentApps:true}};
const normalizeContext=(value:unknown):CopilotRecentContext=>{const input=value&&typeof value==='object'?value as Record<string,unknown>:{};return{lastPage:typeof input.lastPage==='string'?input.lastPage.slice(0,240):null,lastApplication:typeof input.lastApplication==='string'?input.lastApplication.slice(0,160):null,lastPageTitle:typeof input.lastPageTitle==='string'?input.lastPageTitle.slice(0,180):null,recentApplications:Array.isArray(input.recentApplications)?input.recentApplications.filter((x):x is string=>typeof x==='string').slice(0,8):[]}};
const hydrate=(row:any):CopilotProfile=>({id:String(row.id),organizationId:String(row.organizationId),userId:String(row.userId),profileVersion:Number(row.profileVersion||1),identitySnapshot:row.identitySnapshot||{displayName:null,workEmail:null,employeeUsername:null,role:''},preferences:normalizePreferences(row.preferences),recentContext:normalizeContext(row.recentContext),createdAt:row.createdAt,updatedAt:row.updatedAt,lastSeenAt:row.lastSeenAt});
export async function ensureSIACopilotProfile(prisma:PrismaClient,auth:AuthContext,options:{page?:string|null;application?:string|null;pageTitle?:string|null}={}):Promise<CopilotProfile>{await ensureProfileStorage(prisma);const existing=await prisma.$queryRawUnsafe<any[]>(`SELECT * FROM "SIAEmployeeProfile" WHERE "organizationId"=$1 AND "userId"=$2 LIMIT 1`,auth.organizationId,auth.userId);const previous=existing[0]?hydrate(existing[0]):null;const preferences=previous?.preferences||DEFAULT_PREFERENCES;const recent=previous?.recentContext||DEFAULT_CONTEXT;const application=options.application?.trim().slice(0,160)||recent.lastApplication;const nextContext={lastPage:options.page?.trim().slice(0,240)||recent.lastPage,lastApplication:application||null,lastPageTitle:options.pageTitle?.trim().slice(0,180)||recent.lastPageTitle,recentApplications:preferences.rememberRecentApps&&application?[application,...recent.recentApplications.filter(v=>v!==application)].slice(0,8):recent.recentApplications};const identitySnapshot:IdentitySnapshot={displayName:auth.email||null,workEmail:auth.email?.toLowerCase()||null,employeeUsername:null,role:auth.role};const id=previous?.id||randomUUID();const rows=await prisma.$queryRawUnsafe<any[]>(`INSERT INTO "SIAEmployeeProfile" ("id","organizationId","userId","profileVersion","identitySnapshot","preferences","recentContext","createdAt","updatedAt","lastSeenAt") VALUES ($1,$2,$3,1,$4::jsonb,$5::jsonb,$6::jsonb,NOW(),NOW(),NOW()) ON CONFLICT ("organizationId","userId") DO UPDATE SET "identitySnapshot"=EXCLUDED."identitySnapshot","preferences"=EXCLUDED."preferences","recentContext"=EXCLUDED."recentContext","updatedAt"=NOW(),"lastSeenAt"=NOW() RETURNING *`,id,auth.organizationId,auth.userId,JSON.stringify(identitySnapshot),JSON.stringify(preferences),JSON.stringify(nextContext));return hydrate(rows[0]);}
export function serializeSIACopilotProfile(profile:CopilotProfile):string[]{return[`serverConfirmedSIACopilotProfileVersion: ${profile.profileVersion}`,`serverConfirmedSIACopilotDisplayName: ${profile.identitySnapshot.displayName||'NOT_FOUND'}`,`serverConfirmedSIACopilotPreferredName: ${profile.preferences.preferredName||'NOT_SET'}`,`serverConfirmedSIACopilotResponseStyle: ${profile.preferences.responseStyle}`,`serverConfirmedSIACopilotRecentApplications: ${profile.recentContext.recentApplications.join(' | ')||'NONE'}`]}
export function registerSIACopilotProfileRoutes({app,prisma,authOf,requireRoles}:Dependencies){const gate=requireRoles(...allRoles);app.get('/api/sia/profile',gate,async(_req,res,next)=>{try{res.json({data:{profile:await ensureSIACopilotProfile(prisma,authOf(res))}})}catch(e){next(e)}});app.patch('/api/sia/profile',gate,async(req,res,next)=>{try{const auth=authOf(res);const input=updateSchema.parse(req.body);const profile=await ensureSIACopilotProfile(prisma,auth);const preferences={...profile.preferences,...input,preferredName:input.preferredName===undefined?profile.preferences.preferredName:input.preferredName};await prisma.$executeRawUnsafe(`UPDATE "SIAEmployeeProfile" SET "preferences"=$1::jsonb,"updatedAt"=NOW() WHERE "organizationId"=$2 AND "userId"=$3`,JSON.stringify(preferences),auth.organizationId,auth.userId);res.json({data:{profile:await ensureSIACopilotProfile(prisma,auth)}})}catch(e){next(e)}});app.post('/api/sia/profile/context',gate,async(req,res,next)=>{try{const auth=authOf(res);const input=contextSchema.parse(req.body);res.json({data:{profile:await ensureSIACopilotProfile(prisma,auth,input)}})}catch(e){next(e)}})}